- let items = information.general.items
div
  each item,i in items
    if i === 0 && !item.name
      = '--'
    else
      //-a(href=item.link title=item.info||'')= item.name
      =`[url=http://naruto-role-play-rpg.forumotion.com${item.link}]${item.name}[/url]`
      br
